/*global describe, it */
'use strict';
var assert = require('assert');
var dPaccomparativeJudgement = require('../');

describe('d-paccomparative-judgement node module', function () {
  it('must have at least one test', function () {
    dPaccomparativeJudgement();
    assert(false, 'I was too lazy to write any tests. Shame on me.');
  });
});
